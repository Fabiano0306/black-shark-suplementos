[build]
  command = "npm run build"
  publish = "dist"

# ⚙️ Define o ambiente Node correto
[build.environment]
  NODE_VERSION = "18"
  NPM_VERSION = "9"

# 🔁 Redirecionamento para SPA (React Router)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
